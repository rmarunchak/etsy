default: -r features --format junit --out features/reports --format pretty -r features --format json --out series_run<%= ENV['TEST_ENV_NUMBER']%>.json --strict --format AllureCucumber::CucumberFormatter --out report/allure